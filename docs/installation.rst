============
Installation
============

At the command line::

    $ easy_install django-rest-localflavor

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-localflavor
    $ pip install django-rest-localflavor
